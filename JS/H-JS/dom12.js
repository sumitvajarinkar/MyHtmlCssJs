// Synchronous vs Asynchronous
// Asynchronous allow multiple things at the same time
// Async/await
//Callbacks
//promises

// Synchronous allow one at a time

setTimeout(() => {
    for (let index = 0; index < 100; index++) {
        const element = [index];
        // console.log('This is index number' + index)
    }
    
}, 100);

// console.log('Done')



